# 复制为 .env（同目录，docker compose 会自动读取），按需修改。

# 媒体数据在宿主机上的目录（会以只读方式挂到容器的 /data/media）。
# 默认 ./sample_data（项目内）；放到项目外请指绝对路径，例如：
# POLYSEEK_MEDIA_DIR=/data/polyseek-media
POLYSEEK_MEDIA_DIR=./sample_data
